[phases.setup]
nixPkgs = ["ffmpeg", "python311", "python311Packages.pip"]

[phases.install]
cmds = [
    "pip install --user yt-dlp openai-whisper faster-whisper",
    "whisper --help || echo 'Whisper installed'"
]
